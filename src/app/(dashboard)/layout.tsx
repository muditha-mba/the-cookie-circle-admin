import type { ReactNode } from "react";

/**
 * Dashboard route group layout.
 * Protected route enforcement will be added in Phase 2.
 */
export default function DashboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
